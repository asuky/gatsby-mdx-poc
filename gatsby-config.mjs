/**
 * @type {import('gatsby').GatsbyConfig}
 */
const config = {
  siteMetadata: {
    title: `mdx-poc`,
    siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [
    `plugin-mdx-poc`
  ],
};

export default config;